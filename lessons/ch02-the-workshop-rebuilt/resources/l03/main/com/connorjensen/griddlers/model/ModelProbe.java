package com.connorjensen.griddlers.model;

// Boundary fixture - matrix rows 8 and 9. `model` is neither the engine root nor `tools`.
// Copy to src/main/java/com/connorjensen/griddlers/model/, run `mvn -B checkstyle:check`, delete.

import com.connorjensen.griddlers.tools.TerminalGriddler;
import java.io.File;

class ModelProbe {}
